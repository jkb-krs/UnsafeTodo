[package]
name = "unsafe_todo"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_cbor = "0.11"
dirs = "5.0"  # für plattformunabhängigen Pfad
